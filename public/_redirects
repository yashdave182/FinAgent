# Netlify _redirects file for SPA routing
# This file must be in the public/ directory so Vite copies it to dist/

# Don't redirect assets, images, fonts, or other static files
/assets/*  /assets/:splat  200
/images/*  /images/:splat  200
/fonts/*   /fonts/:splat   200
/vite.svg  /vite.svg       200

# SPA fallback - all other routes serve index.html
/*  /index.html  200
